{
  "name": "quran-android-app",
  "version": "1.0.0",
  "description": "المصحف الرقمي - أكاديمية أزهريون",
  "scripts": {
    "build": "echo 'Static app - no build needed'",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
